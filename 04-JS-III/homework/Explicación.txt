Un array, es un tipo de objeto que estructura en un orden los elementos que se le asignen, pueden contener datos homogeneos pero en js pueden tener diferentes tipos. el array puede guardar otros arrays, variables de tipo primitivo, objetos, funciones entre otros, agrupando todos estos datos dentro de una "colección" estructurada. Se podran ingresar a los datos del array con el indice de cada uno de los elementos desde el 0 en adelante.